import { Text, View } from 'react-native';

export function MobileNotificationsScreen() {
  return (
    <View style={{ padding: 16 }}>
      <Text>Notification center (in-app + push event history).</Text>
    </View>
  );
}
